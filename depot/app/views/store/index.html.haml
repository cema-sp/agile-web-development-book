- if notice
  %p#notice= notice

%h1= t('.catalog')


- @products.each do |product|
  .entry
    = image_tag(product.image_url)
    %h3= product.title
    %p= sanitize(product.description)
    .price_line
      %span.price
        = number_to_currency(product.price*StoreController::EXCHANGE_RATES[I18n.locale])
        = button_to t('.add'), 
          line_items_path(product_id: product), 
          remote: true